language: haxe

before_install:
  - wget http://download.macromedia.com/pub/flex/sdk/flex_sdk_4.6.zip
  - unzip flex_sdk_4.6.zip

install:
  - git clone --branch v2.4.1 --depth 1 https://github.com/google/protobuf.git
  - make protobuf/src/protoc

before_script:
  - |
    echo '
    PROTOBUF_VERSION=2.4.1
    PROTOBUF_DIR=protobuf
    ifeq ($(OS), Windows_NT)
    BAT=.bat
    EXE=.exe
    PATH_SEPARATOR=;
    else
    PATH_SEPARATOR=:
    endif
    COMPC=flex_sdk_4.6/bin/compc$(BAT)
    MXMLC=flex_sdk_4.6/bin/mxmlc$(BAT)
    ASDOC=flex_sdk_4.6/bin/asdoc$(BAT)
    FDB=flex_sdk_4.6/bin/fdb$(BAT)
    MVN=mvn
    JAVAC=javac
    JAR=jar
    HAXE=haxe
    ' > config.mk

script:
  - make
